<?xml version="1.0" encoding="utf-8"?>
<!--
    Shared "you need an account for this" state.

    Included by the Trips and Community tabs, which are both a FrameLayout holding a list.
    Keeping it in one file means the three places that ask for a sign-in look the same and
    only the message differs, set from code.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/signedOutPanel"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:gravity="center"
    android:orientation="vertical"
    android:padding="32dp"
    android:visibility="gone">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="🔒"
        android:textSize="46sp" />

    <TextView
        android:id="@+id/signedOutMessage"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:gravity="center_horizontal"
        android:lineSpacingExtra="4dp"
        android:textColor="@color/text_secondary"
        android:textSize="15sp" />

    <com.google.android.material.button.MaterialButton
        android:id="@+id/signedOutSignInButton"
        style="@style/Widget.Journify.Button"
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:layout_marginTop="24dp"
        android:text="@string/login_button"
        app:cornerRadius="18dp" />
</LinearLayout>
